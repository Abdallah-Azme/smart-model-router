{
  "skill_name": "smart-model-router",
  "fixture": "A miniature multi-tenant Laravel modular monolith with an Inertia/React frontend (generated by the workspace make_fixture.py). Planted findings are recorded in the workspace answer_key.json, which executors never see.",
  "assertion_buckets": "Each assertion measures cost (did it avoid waste), process (did it route and compress deliberately), or outcome (did the work come out right). Iteration 1 showed a process-heavy mix inflates the apparent benefit of the skill, because the skill instructs a route announcement and a baseline has no reason to write one. Iteration 2 rebalances toward cost and outcome, and rewrites assertions that a run could satisfy vacuously by delegating nothing at all.",
  "evals": [
    {
      "id": 0,
      "name": "productstatus-usage-sweep",
      "prompt": "We are renaming ProductStatus::Archived to ProductStatus::Retired next sprint. Before I commit to that, find every place ProductStatus is used or duplicated in this repo and tell me which files would need updating. Do not change anything yet.",
      "expected_output": "A file-by-file inventory covering all 14 sites including the three non-obvious ones (config/catalog.php bare strings, the duplicated TypeScript union in resources/js/types/product.ts, the migration column default 'draft'), produced with deterministic search, no Opus, and ending in a completeness check rather than an assurance.",
      "expectations": [
        "Opus is not used anywhere in the route",
        "The search is done with deterministic tools (grep/Grep/Glob) rather than by reading files one at a time to find references",
        "The report identifies at least 11 of the 14 known ProductStatus sites",
        "The report identifies the duplicated TypeScript union in resources/js/types/product.ts",
        "The report identifies the bare status strings in config/catalog.php",
        "The report identifies the literal 'draft' default in the create_products_table migration",
        "The response gives a deterministic completeness check for the eventual rename (for example: grep the old name and show zero hits) rather than only asserting the list is complete",
        "No fixture source file is modified, since the user asked for a report only"
      ]
    },
    {
      "id": 1,
      "name": "products-endpoint-filter-sort",
      "prompt": "Implement filtering and sorting for the products endpoint. Filter by status and name, sort by name, price and created_at, and keep it consistent with how we do things elsewhere.",
      "expected_output": "Sonnet-led implementation following the CLAUDE.md convention (spatie/laravel-query-builder, paginated resource collection), validation updated, a test added. No Opus, and no orchestration ceremony: this is work inside an existing API contract.",
      "expectations": [
        "Opus is not used anywhere in the route",
        "The implementation follows the project convention of spatie/laravel-query-builder stated in CLAUDE.md",
        "Both filtering and sorting are implemented in ProductController",
        "Request validation is updated to cover the new filter and sort parameters",
        "A test covering sorting or the new filters is added or updated",
        "The work is not split across more than two delegated steps, and there is no redundant second recon pass",
        "Verification is attempted and its real result reported, rather than a pass being implied"
      ]
    },
    {
      "id": 2,
      "name": "inventory-reservation-redesign",
      "prompt": "Redesign our inventory reservation lifecycle to prevent overselling under concurrent checkout. Two customers buying the last unit at the same moment must not both succeed.",
      "expected_output": "The concurrency and transaction-boundary decision is treated as contract-level and given to Opus (as designer or as reviewer of the diff), on a compact packet rather than the repository, with Sonnet implementing. Must find the read-compare-write race in ReserveStock and the reservation-outside-transaction problem in PlaceOrder, and land a concrete mechanism plus a contention test.",
      "expectations": [
        "The invariants / concurrency / transaction-boundary decision is routed to Opus, either as the designer or as a reviewer of the implemented diff",
        "Opus receives a compact packet or findings summary — an objective, the relevant findings or constraints, the risk, and the specific question — rather than raw files or the repository",
        "The unguarded read-compare-write in app/Modules/Inventory/Actions/ReserveStock.php is identified",
        "The fact that PlaceOrder reserves stock outside the DB::transaction that creates the order is identified",
        "The implementation work itself is done by Sonnet rather than Opus",
        "A concrete concurrency mechanism is implemented (row lock / lockForUpdate, atomic conditional UPDATE, or unique reservation rows) rather than a vague recommendation",
        "A test that actually exercises contention for the last unit is written, not merely a single-threaded reserve test",
        "Verification is attempted and its real result reported, rather than a pass being implied"
      ]
    },
    {
      "id": 3,
      "name": "five-type-errors",
      "prompt": "npm run types is failing with 5 errors, all in resources/js/pages/Products/Index.tsx. Fix them.",
      "expected_output": "Straight to Sonnet. No Opus, no recon subagent, and no routing ceremony — the compiler already located the errors. All 5 planted type errors fixed, type checker re-run or its re-run explicitly called for.",
      "expectations": [
        "Opus is not used anywhere in the route",
        "No exploration or recon subagent is spawned; the work is done directly",
        "All five planted type errors in Index.tsx are fixed (invalid 'retired' status, formatPrice given a string, p.title instead of p.name, meta.total compared to '0', async onClick handler)",
        "Re-running the type checker (npm run types / tsc --noEmit) is performed or explicitly called for, and its real result reported",
        "The run does not pay routing overhead disproportionate to the task: it does not read the skill's reference files for a task this routine",
        "Fixes address the real contracts in the codebase (for example using the existing initialStatus prop or a valid ProductStatus value) rather than papering over the type error"
      ]
    },
    {
      "id": 4,
      "name": "tenancy-authz-audit",
      "prompt": "Audit this multi-tenant authorization architecture for possible tenant data leakage. I need to know if one tenant can ever see another tenant's data.",
      "expected_output": "Cheap, focused recon over the independent surfaces (policies, global scopes, background paths) — delegated if the surface warrants it, read directly if it does not — compressed for an Opus judgment on exploitability, then Sonnet fixes plus a cross-tenant denial test. Must find the ExportOrdersJob leak and the fail-open scope.",
      "expectations": [
        "The expensive reasoning / risk-ranking step is routed to Opus",
        "The recon is focused rather than unbounded: either delegated to cheap workers with narrow independent questions, or done directly with a stated reason that the surface is small enough to read — and if delegated, no more than five workers are used",
        "Opus receives a compressed packet or findings summary rather than whole files or the repository",
        "The ExportOrdersJob cross-tenant leak (withoutGlobalScopes with no tenant filter) is identified",
        "The fail-open behavior of the BelongsToTenant global scope in unauthenticated contexts such as queue jobs is identified",
        "The background/queue paths are covered, not only the HTTP request path",
        "A test asserting cross-tenant denial is recommended or written"
      ]
    },
    {
      "id": 5,
      "name": "dto-rename-no-orchestration",
      "prompt": "Rename ProductFilterData to ProductQueryFilters and update its imports.",
      "expected_output": "Done directly, cheaply, and completely. No Opus, no delegation chain, no reference reading, no routing announcement. Rename applied across all three referencing files including the test filename, and proven complete by grepping the old name.",
      "expectations": [
        "Opus is not used anywhere in the route",
        "No subagent is delegated for this task",
        "The class is renamed in app/Modules/Catalog/DTO/ and the file is renamed to match",
        "All references are updated in ProductController and in the unit test, with zero remaining occurrences of the old name",
        "Completeness is proven deterministically (grep for the old name returning zero hits) rather than asserted",
        "The run does not pay routing overhead disproportionate to the task: it does not read the skill's reference files, and does not manufacture a routing announcement for work it did itself",
        "Total tool calls stay proportionate to a three-file rename (16 or fewer)",
        "The response does not propose an architecture discussion or redesign for this rename"
      ]
    },
    {
      "id": 6,
      "name": "large-repo-order-audit",
      "prompt": "Audit this repository's order architecture. Assume it is much larger than what you can read in full, so be smart about how you go about it.",
      "expected_output": "The claim that the repo is large is checked rather than accepted. Because the order surface is in fact small, reading it directly and saying so is the correct, cheapest answer; delegating narrow independent questions is also acceptable. Either way, no Opus on file-reading, and at least two real findings with file references.",
      "expectations": [
        "Opus is not used for the file-reading or reconnaissance work",
        "The scope of the order surface is established before expensive reasoning begins, rather than reasoning from an unexamined assumption that the repo is huge",
        "Recon is proportionate: either the surface is read directly with a stated reason that it is small enough, or it is split into no more than five narrow independent questions — unfocused 'explore the codebase' delegation fails",
        "At least two real order-architecture findings are reported (stock reserved outside the order transaction, no compensating release on failure, no order state-machine transition guards, or ExportOrdersJob reaching into Order models from outside the module)",
        "Findings are reported with file references rather than as generic advice",
        "Any expensive reasoning step receives a compressed findings summary rather than raw files"
      ]
    },
    {
      "id": 7,
      "name": "intermittent-checkout-failure",
      "prompt": "Checkout intermittently fails in production with insufficient stock errors, but it works fine locally and stock levels look correct in the database. Figure out what is going on.",
      "expected_output": "Evidence before theory, cheap tiers first, and escalation only if the evidence points at concurrency or ordering. Should surface the reservation leak (reserved is incremented and never released) as the leading hypothesis, with a reproduction or regression test proposed.",
      "expectations": [
        "The route does not begin with Opus",
        "What evidence would distinguish the hypotheses is named (logs, traces, failing requests, recent deploys), even though production logs are unavailable here",
        "Any escalation to Opus follows the evidence rather than being automatic or pre-emptive",
        "The reservation leak (reserved incremented on every checkout with ReleaseStock never called) is named as the leading hypothesis",
        "The race in ReserveStock is distinguished from the leak rather than the two being conflated, since they predict different symptoms",
        "A reproduction or regression test is proposed before or alongside the fix"
      ]
    },
    {
      "id": 8,
      "name": "destructive-column-drop",
      "prompt": "We want to drop the reserved column from stock_levels and track reservations in a new stock_reservations table instead. Write the migration. We are deploying to production on Friday.",
      "expected_output": "Recognises destructive, irreversible work against production data and a change to the reservation contract. Opus plans or reviews on those grounds; Sonnet writes the migration; the drop is deferred behind an expand-and-contract sequence with a rollback path. Must not treat it as routine because the diff is short.",
      "expectations": [
        "The response explicitly identifies this as destructive / hard to reverse against production data, rather than treating it as a routine migration",
        "Opus either plans the sequencing or reviews it before execution, on stated grounds of irreversibility or production data risk",
        "A staged expand-and-contract sequence is proposed (add and backfill before dropping) rather than a single destructive migration",
        "A rollback or recovery path is addressed",
        "The existing readers and writers of the reserved column are found before planning, rather than the drop being planned blind",
        "The migration code itself is written by Sonnet rather than Opus",
        "The backfill is safe to run against live data (idempotent, and not dependent on request-scoped state such as an authenticated user)"
      ]
    },
    {
      "id": 9,
      "name": "many-files-inside-contract",
      "prompt": "Add created_by and updated_by audit columns to products, orders, order_lines and stock_levels, populate them from the authenticated user automatically, and expose them on the API resources. It probably touches 15 files or so.",
      "expected_output": "Sonnet, despite the advertised file count, because the work is additive, pre-production and entirely inside existing conventions. Should reuse the BelongsToTenant trait as prior art, and inherit its known trap (auth-dependent population fails silently in queue contexts). No Opus.",
      "expectations": [
        "Opus is not used anywhere in the route, despite the file count mentioned in the prompt",
        "The response reasons that file count does not raise the tier, or equivalently that this is work inside existing conventions",
        "The existing BelongsToTenant trait is identified as prior art and the new behavior follows that pattern",
        "Migrations, the auto-population mechanism, and the API resources are all covered",
        "The auth-dependent population trap is acknowledged (populating from Auth::user() silently records nothing in queue, console or scheduled contexts)",
        "Verification is attempted and its real result reported, rather than a pass being implied",
        "The run does not pay routing overhead disproportionate to the task: no unnecessary recon delegation for work whose prior art is a single known trait"
      ]
    }
  ]
}
